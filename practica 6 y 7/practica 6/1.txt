Completar:

Consulta a una base de datos: Para comenzar la comunicación con un servidor de base de datos MySQL, es 
necesario abrir una conexión a ese servidor. Para inicializar esta conexión, PHP ofrece la función  
1- "mysqli_connect()"

Todos sus parámetros son opcionales, pero hay tres de ellos que generalmente son necesarios:  
2- "Servidor, usuario y contraseña"

Una vez abierta la conexión, se debe seleccionar una base de datos para su uso, mediante la función  
3- "mysqli_select_db()"

Esta función debe pasar como parámetro  
4- "La coneccion ($link) y el nombre de la base de datos"

La función mysqli_query () se utiliza para  
5- "Ejecutar una consulta SQL sobre la base de datos"

y requiere como parámetros  
6- "La conexion ($link) y la consulta SQL($sql)"

La cláusula or die() se utiliza para  
7- "Finalizar la ejecución del programa y mostrar un mensaje de error si la consulta falla"

y la función mysqli_error () se puede usar para 
8- "Obtener el mensaje de error generado por MySQL"

Si la función mysqli_query() es exitosa, el conjunto resultante retornado se almacena en una variable, por ejemplo 
$vResult, y a continuación se puede ejecutar el siguiente código (explicarlo):
    
    <?php 
        while ($fila = mysqli_fetch_array($vResultado))  
        { 
        ?> 
            <tr> 
            <td><?php echo ($fila[0]); ?></td> 
            <td><?php echo ($fila[1]); ?></td> 
            <td><?php echo ($fila[2]); ?></td> 
            </tr>    
            <tr> 
            <td colspan="5"> 
        <?php 
        } 
    mysqli_free_result($vResultado); 
    mysqli_close($link);
    ?>

9-"Este código recorre y muestra los registros obtenidos de una consulta a la base de datos, luego mysqli_free_result($vResultado) libera la memoria ocupada por los resultados de la consulta y mysqli_close($link) cierra la coneccion con la base de datos"
